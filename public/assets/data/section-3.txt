introduction to equations
evaluating expressions
inverse operations
simple equations
balancing equations
equations with subscripts
word problems into equations
consecutive integers





cost = [5_000, 6_000, 7_500, 5_000]


data = Object.values(document.getElementsByClassName('css-eit09k e6tlfg22'))

let buffer = [];
for (let i = 0; i < indexes.length; i++) {
    buffer.push(data[indexes[i]]);
}

indexes = data.map((x, index) => x.innerText.includes("Oran") ? index : false).filter(y => typeof(y) == 'number')

4512 Hillside ave 